"use client";

import React from "react";

const Home = (): React.ReactElement => {
  // AuthProvider handles all redirects based on auth state
  // This page will never actually render - users will be redirected
  return <></>;
};

export default Home;
