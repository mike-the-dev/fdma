export type StripeRedirectSessionDto = {
  sessionId: string;
  customerName: string;
  companyName: string;
  email: string;
  stripeId: string;
  status: string;
  createdAt: string;
  expiresAt: string;
};
